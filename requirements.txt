Flask>=2.3,<3.0
Flask-Babel>=4.0,<5.0
Flask-Cors>=4.0,<5.0
pyserial>=3.5
Pillow>=10.0
arabic-reshaper>=3.0
python-bidi>=0.4.2
pyusb>=1.2
requests>=2.28
websocket-client>=1.6  # Required for NeoLeap Mada payment terminal driver

# Optional — install for native CUPS printing (Linux only, requires libcups2-dev)
# pycups>=2.0
