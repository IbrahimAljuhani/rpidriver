Flask>=2.3
Flask-Babel>=4.0,<5.0
Flask-Cors>=4.0,<5.0
pyserial>=3.5
Pillow>=10.0
arabic-reshaper>=3.0
python-bidi>=0.4.2
pyusb>=1.2
requests>=2.28

# ── Optional extras ───────────────────────────────────────────────────────────
# NeoLeap Mada payment terminal — install if using neoleap_driver:
#   pip install websocket-client>=1.6
# websocket-client>=1.6

# Native CUPS printing (Linux only — requires: sudo apt install libcups2-dev):
#   pip install pycups>=2.0
# pycups>=2.0
